# Dayshift 9.21 收敛改造记录

> 改造日期：2026-09-21  
> 改造主题：可靠性收敛、流程防循环、产品边界简化  
> 当前状态：代码已完成并通过本地自动化验证，未提交、未部署，真实 DeepSeek/高德接口未自动消耗额度测试。

## 一、改造目标

本轮不继续扩充功能，而是将 Dayshift 收敛为一个边界清晰、能够稳定结束的产品：

- 服务范围限定为中国境内、单日、基于已有正式行程的动态调整。
- 不负责预算、预订、社交、登录、云同步、多日规划、海外路线或长期兴趣推荐。
- 任意请求必须在有限时间内得到明确结果，不允许无限补问或反复解析。
- 无效输入不得调用地图和 Planner。
- 用户补充一个字段时，只更新该字段，不得污染其他已确认事实。
- 原行程只有在用户接受方案并且接受前复验成功后才允许更新。
- 默认只展示一个推荐方案，只有取舍真正不同时才展示一个备选方案。

## 二、数据模型与浏览器会话

### 2.1 Session 升级至 v3

- 浏览器会话版本升级为 `schemaVersion: 3`。
- 新增 `resolutionState`，用于记录：
  - 当前阻塞问题的稳定 key；
  - 同一问题出现次数；
  - 总补充轮数；
  - 已回答字段；
  - 历史问题 key。
- 新增 `stateCapturedAt`，记录当前状态的确认时间。
- 系统时间来源为系统时，重新打开旧会话会刷新当前时间。
- 用户明确提供的当前时间优先，不会被系统时间覆盖。

### 2.2 v2 → v3 迁移

- 旧会话在浏览器加载时迁移一次。
- 保留日期、地点、活动、固定状态和 revision。
- 将行程统一收敛为单日。
- 删除旧预算和费用字段。
- 迁移失败时保留原始 localStorage 备份，并创建干净的 v3 Session，避免静默覆盖用户数据。

### 2.3 完整移除预算域

从 Profile、TripState、活动、语义解析、Planner、Validator、页面和测试中移除了：

- `dailyBudget`
- `remainingBudget`
- `estimatedCost`
- `estimatedCostKnown`
- 预算调整入口
- 泰铢和费用未知提示

高德路线返回的原始 `fare` 字段仍可作为原始世界数据存在，但不参与产品判断、方案取舍或 UI 展示。

## 三、状态机和统一业务响应

### 3.1 持久化阶段

Session 只持久化以下业务阶段：

- `NO_ITINERARY`
- `HAS_ITINERARY`
- `NEEDS_INPUT`
- `PLAN_READY`
- `OUT_OF_SCOPE`
- `UNAVAILABLE`
- `NO_SAFE_PLAN`

解析中、地图查询中、规划中等临时状态不再写入 Session，避免页面刷新后卡在虚假的“处理中”。

### 3.2 `/api/assist` 响应

业务结果统一为：

- `READY`
- `NEEDS_INPUT`
- `OUT_OF_SCOPE`
- `UPSTREAM_UNAVAILABLE`
- `NO_SAFE_PLAN`

失败结果会明确提供失败阶段、是否可重试和用户可执行的提示。

### 3.3 字段级补充

- 首次请求使用 `{ snapshot, rawText }`。
- 后续补充使用 `{ snapshot, confirmedDraft, answer, resolutionState }`。
- `answer` 支持文本、时间、POI、浏览器定位、活动选择和交通方式。
- 后续回答通过确定性字段更新合并到草稿，不再把“原文 + 用户回答”拼接后重新解析全文。
- POI 选择只能使用当前 blocker 返回的合法候选项。
- 补充回答不会改变其他活动 ID、时间、地点和固定状态。

## 四、防循环和输入门控

### 4.1 输入门控

只有以下输入可以进入 Grounding 和 Planner：

- 明确发生了行程变化；
- 用户明确要求优化或重排行程；
- 用户明确修改某个活动。

空白、闲聊、乱码、emoji、“不靠谱”“试一下”等输入不会进入地图或 Planner，而是直接返回范围外结果或一个明确问题。

删除了“没有 disruption 就默认 optimize”的旧逻辑。

### 4.2 补充轮数限制

- 每次只返回一个 blocker。
- 同一 blocker 第一次允许自由回答。
- 第二次转为结构化输入或候选选择。
- 同一 blocker 第三次出现时安全结束。
- 整个补充流程超过三轮时安全结束。
- 结束结果为 `OUT_OF_SCOPE` 或 `NO_SAFE_PLAN`，原正式行程保持不变。

## 五、编排、超时和失败恢复

### 5.1 新编排链路

首次输入：

1. Parser
2. 输入门控
3. 合并确认事实
4. Grounding
5. Planner
6. Validator

后续补充：

1. 确定性字段更新
2. Grounding
3. Planner
4. Validator

除非用户明确要求重新理解整段文字，否则后续回答不会再次调用 Parser。

### 5.2 超时限制

- Parser：8 秒。
- Planner：单轮 10 秒，最多两轮。
- 高德单次请求：8 秒。
- `/api/assist` 总 deadline：30 秒。
- 取消信号会继续传递到语义解析、地图服务和 Planner。

### 5.3 地图查询收敛

- 优先查询当前位置到下一活动。
- 查询相邻保留活动之间的必要路线。
- 查询固定预约涉及的必要路段。
- 替代地点只查询进入候选所需的路线。
- 删除地点全量笛卡尔积式路线查询。

### 5.4 失败恢复

- Parser、地图或 Planner 失败时不会修改正式行程。
- 保留已确认草稿和已成功阶段的结果。
- 返回失败阶段、是否可重试和用户可行动文案。
- 不要求用户从第一步重新输入全部内容。

## 六、页面流程收敛

### 6.1 首页

首页只负责：

- 首次描述变化；
- 单个字段补充；
- POI/活动候选选择；
- 浏览器定位；
- 上游失败后的返回修改或重试。

复杂活动编辑统一进入 `/rescue`。

### 6.2 创建页

- 只负责创建或编辑正式单日行程。
- 不自动触发规划。
- 删除预算输入和复杂长期偏好。
- 仅保留节奏、步行承受度和本次明确交通限制。

### 6.3 救援页

- 初始化只使用已有解析结果，必要时调用一次 `/api/parse`。
- “确认并生成方案”后才调用一次 `/api/assist`。
- 不再在补充过程中重复拼接文本并重新理解全文。

### 6.4 今日页

- 根据当前系统时间重新计算活动状态。
- 未查询天气时显示“天气未查询”。
- 已过期但未确认完成的活动显示“时间已过，待确认”。
- 不再强制把第一项活动标记为“进行中”。

### 6.5 结果页

- 默认展示一个推荐方案。
- 最多展示一个取舍真正不同的备选方案。
- 保留不可行方案的冲突解决入口。
- 删除“再少安排点 / 再多安排点”等模糊二次规划入口。
- 接受方案前必须调用 `/api/validate` 重新 Grounding，并检查世界数据新鲜度和 base revision。
- 只有复验成功后才更新正式行程和 revision。

### 6.6 加载体验

- 显示统一、诚实的处理中提示。
- 显示已等待时间。
- 提供取消和返回修改。
- 没有真实阶段事件时，不伪造“正在查路线”等精确进度。

## 七、接口和运行时清理

生产页面现在只依赖：

- `/api/parse`
- `/api/assist`
- `/api/validate`
- `/api/config`

已删除：

- `/api/replan`
- `/api/world/context`
- `/api/world/places`
- 旧确定性 Planner
- 旧 Prompt
- 旧输入解析器和救援意图模块
- 本地地点目录的生产运行路径
- 预算 Validator
- 生产 Demo 快照运行路径

仍需要的 decision trace 已拆分到独立服务中。

## 八、自动化测试

### 8.1 可靠性测试

新增或重写了以下验证：

- v2 会话迁移到 v3，并清理预算字段。
- 无效输入不会调用 Grounding 或 Planner。
- 字段级补充只更新目标字段。
- 同一 blocker 和总补充轮数有明确上限。
- Planner 最多执行两轮。
- 推荐方案最多带一个备选方案。
- 固定活动不得被删除或篡改。

### 8.2 Playwright E2E

新增标准 Playwright 测试，覆盖：

- 首次创建只保存单日正式行程。
- 地点候选只作为当前 blocker 的字段答案提交。
- 上游失败时保留正式行程并展示可行动错误。
- 接受方案前复验成功后才更新 revision。

Playwright 已接入默认测试命令。测试会先完成生产构建，再启动生产模式应用执行浏览器链路。

## 九、最终验证结果

本轮完成后已执行：

- TypeScript 类型检查：通过。
- ESLint：通过。
- 可靠性单元/集成测试：通过。
- Next.js 生产构建：通过。
- Playwright E2E：4 条全部通过。
- 预算、费用、泰铢源码扫描：无残留。
- 旧补充阶段和旧模糊调整参数扫描：无残留。
- 生产 Demo 运行路径扫描：无残留。

## 十、本轮刻意未做

- 未部署。
- 未提交 Git commit。
- 未新增账号、认证、云同步或公网限流。
- 未增加多日规划、海外路线、预算、预订或社交能力。
- 未自动执行真实 DeepSeek/高德测试，避免消耗额度；保留并更新了手工 live 测试脚本。
- 类型名称重命名未与本轮行为修改混在一起，后续如需要，可单独做无行为变更提交。

## 十一、工作区说明

- 用户原本已经删除的 `项目拆解讲解.html` 继续保持删除状态。
- 本轮没有恢复、修改或提交该无关文件。
- 当前改造仍位于工作区，正式提交前建议先人工体验一轮真实 API 链路，再按“模型/地图故障”和“正常调整”分别验收。
