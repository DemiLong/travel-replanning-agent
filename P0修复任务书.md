# CoveredYou P0 修复任务书

> 给实施者（Codex）的任务说明。
> 产品定位见同目录 `CoveredYou.md`，所有改动必须符合该定位。
> 权限：只读改为可读可写，仅改本任务书列出的范围；不要顺手重构无关代码。
> 完成后不部署、不 push、不改生产数据。

---

## 一、P0 目标

修通这一条主流程：

> 用户在街头输入"出了什么事 + 我接下来要干嘛" → 系统识别出活动 → 最多问一次必要信息 → 出方案。

用下面这句话当试金石，修完必须真实验证通过：

> "现在下雨了，我想3点去印暨小馆喝咖啡的，然后6点去吃金鹏老友粉，晚上8点去看小蛮腰，还来得及全部做这些事吗？"

---

## 二、四项修改

### P0-1：草稿必须包含全部待调整活动（最核心）

**问题**：`agents/agent-orchestrator.ts` 首次请求时，`mergeParsedFacts` 正确合并了已存行程，但随后的 `parsedToDraft`（约第105行）只从 `parsed.existingPlans` 构造草稿，没有把合并后、已存行程里用户这次没提到的活动带进来。第二次请求 `mergeConfirmedDraft`（约第173行）把草稿当成完整集合，导致：
- 非锁定活动被静默丢弃；
- 锁定活动被误报"固定安排不能被静默删除"。

**改法**：
1. 项目里 `services/itinerary-domain.ts` 已经有正确实现：`hydrateParsedPlans`（约第90行）会把已存行程里未完成的活动补回 `existingPlans`，`confirmedDraftFromParsed`（约第116行）用它生成草稿。
2. 让 `agent-orchestrator.ts` 首次请求直接调用这套已有函数，替换掉自己写的 `parsedToDraft`。
3. 统一活动 ID、placeId、locked 状态的保留规则：已存行程里有的活动，补问过程中 ID 和 locked 状态不能变。
4. **不要重新设计合并规则**，不要新写一套。复用已有函数，接线即可。

**涉及文件**：`agents/agent-orchestrator.ts`、`services/itinerary-domain.ts`（如接口需要微调可改，但不要重写语义）。

---

### P0-2：空行程时先识别原计划，不要直接报错

**问题**：`agent-orchestrator.ts` 约第301行，只要 `snapshot.itinerary` 没有未完成活动，就直接返回"当前没有可调整的正式行程，请先到创建页"。

但产品定位是：用户在输入框里说的"3点A、6点B、8点C"本身就是他的原计划，不要求他先去创建页存。

**改法**：
1. 判断"有没有可调整的内容"时，不只看 `snapshot.itinerary`，还要看本次解析出的 `parsed.existingPlans`（含 `activityMentions` 里 role 为 `existing_plan` 的）。
2. 只要解析出了活动，即使浏览器里没有已存行程，也继续走确认 → 补问 → 规划流程，不要直接 OUT_OF_SCOPE。
3. 只有当本次输入确实没有任何活动、也没有已存行程时，才提示"请告诉我你今天想做什么"，并提供创建入口。
4. 普通信息在首页问；复杂编辑可转救援页，但要带上已有草稿，不要让用户重输。

**涉及文件**：`agents/agent-orchestrator.ts`（约第298-303行的门控逻辑）。

---

### P0-3：修正 prompt，不要把用户说的原计划标成"考虑中"

**问题**：`services/semantic-parser.ts` 的 system prompt（约第26-27行）把"旅行者在问要不要做"归为 `considering`。用户说"我想3点去A，还来得及吗"——语气是在问能不能做完，但活动本身是他今天的安排，不是在考虑要不要出门。标成 `considering` 后这些活动不进 `existingPlans`，等于被丢掉。

**改法**：
1. 修改 prompt，明确：用户说了几点、去哪个地点，即使后面跟着"来得及吗""来得及吗""要去吗"这种询问语气，活动本身仍是 `existing_plan`。询问的是可行性/时间够不够，不是"我要不要做这个活动"。
2. 只有用户明确说"我在考虑要不要去X"或"要不要取消X"时，才标 `considering`。
3. 不要为了这个分类去大改 parser 结构，只改 prompt 里的规则描述。

**涉及文件**：`services/semantic-parser.ts`（`SEMANTIC_PARSER_PROMPT`）。

---

### P0-4：最小错误容错，不要把技术错误甩给用户

**问题**：
- `agent-orchestrator.ts` 约第292行，Parser 失败时把 `error.message` 原样返回（比如 "MODEL_OUTPUT_INCOMPLETE"、"not valid JSON"）。
- `components/workflows.tsx` 约第248行直接 `await response.json()`，收到非 JSON 响应时前端崩。

**改法**：
1. 后端：不要把底层异常 message 原样放进给用户的 error。换成固定中文提示（比如"AI 暂时没理解你的话，请换个说法或稍后再试"），错误细节只记到服务端日志。
2. 前端：`response.json()` 外面包 try-catch，解析失败时显示"网络不稳定，请重试"，不要把 "Unexpected token" 之类抛给用户。
3. **这是最小修复**，不要在 P0 阶段做完整错误码体系、诊断日志、失败阶段细分——那些放后面。

**涉及文件**：`agents/agent-orchestrator.ts`、`components/workflows.tsx`。

---

## 三、明确不做什么（边界）

以下事情 P0 一律不做，不要"顺手"改：

- 不做账号、云同步、登录
- 不做情绪安抚
- 不改 UI 风格、不加新页面
- 不重构整体架构、不重命名变量
- 不换模型（不接 Jev 或其他模型）
- 不做多日、海外、预算、预订、社交
- 不改补充轮数上限（现在是3轮，这个后面单独调到2轮，P0 不动）
- 不改时间覆盖逻辑（现在是10分钟换系统时间，后面单独修）
- 不部署、不 push、不删用户已有数据

---

## 四、验收标准

修完后，用真实浏览器、真实 DeepSeek、真实高德跑下面这条输入：

> 现在下雨了，我想3点去印暨小馆喝咖啡的，然后6点去吃金鹏老友粉，晚上8点去看小蛮腰，还来得及全部做这些事吗？

**通过 =**
- 不出现"请先到创建页建立今天的行程"
- 系统识别出三项活动和 15:00 / 18:00 / 20:00
- 不把这三项标成"考虑中"然后丢掉
- 地点有歧义时只问一次，不要求用户重录
- 不出现 "not valid JSON"、"Unexpected token"、堆栈信息
- 如果浏览器里有已存行程，补充信息后原来的活动不丢失

**不通过 =**
- 任何让用户重新录入一遍的情况
- 任何把活动弄丢、把固定预约误判为删除的情况
- 任何把技术错误直接显示给用户的情况

另外跑两个补充场景验证 P0-1：
1. 浏览器里已有几项普通行程，输入"我现在换地方了，在地铁站附近，下雨了"，回答位置后活动不丢失。
2. 浏览器里有一个固定预约（比如18:00集合），同样补充位置后不报错。

---

## 五、交付物

1. 修改后的代码（只动上面列出的文件）。
2. 真实跑通"试金石那句话"的记录：页面截图或关键请求/响应摘要。
3. 修改说明：改了哪几个文件、每处改动一句话说明为什么。
4. 如果某项因为外部原因（API 额度、地点搜不到）没能真实验证，如实说明阻塞点，不要写"已通过"。
